#pragma once
#include <map>
#include "material.h"

namespace resources
{
	/*
	struct defaults
	{
		Material quad_mat, circ_mat;
		Shader quad_shader, circ_shader;
		Texture texture;

		void init()
		{
			// Create default materials
			
			quad_shader.load("res/Shaders/sprite.vs", "res/Shaders/sprite.fs");
			circ_shader.load("res/Shaders/sprite.vs", "res/Shaders/circle.fs");

			texture.load("res/Images/white.png");

			quad_mat.shader = quad_shader;
			quad_mat.texture = texture;

			circ_mat.shader = circ_shader;
			circ_mat.texture = texture;
		}
		void clear();
	};
	*/
}
